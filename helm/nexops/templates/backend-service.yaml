apiVersion: v1
kind: Service
metadata:
  name: nexops-backend
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: nexops-backend
  ports:
    - name: http
      port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}
  type: ClusterIP
